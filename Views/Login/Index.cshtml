@{
    ViewData["Title"] = "Login";
}
@model ViewModel.LoginViewModel
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <h3 class="text-center mt-5">Iniciar Sesión</h3>
            <form method="post" asp-controller="Login" asp-action="Autenticar">
                <div class="mb-3">
                    <label asp-for="@Model.Usuario" class="form-label">Usuario</label>
                    <input asp-for="@Model.Usuario" class="form-control" placeholder="Ingrese su usuario">
                    <span asp-validation-for="@Model.Usuario" class="text-danger"></span>   
                </div>
                <div class="mb-3">
                    <label asp-for="@Model.Password" class="form-label">Contraseña</label>
                    <input asp-for="@Model.Password" type="password" class="form-control" placeholder="Ingrese su contraseña">
                    <span asp-validation-for="@Model.Usuario" class="text-danger"></span>   
                
                </div>
                <div class = "d-flex justify-content-center">
                    <button type="submit" class="btn btn-success">Iniciar Sesión</button>
                </div>
            </form>
        </div>
    </div>
</div>
@section Scripts{<partial name="_ValidationScriptsPartial"/>}